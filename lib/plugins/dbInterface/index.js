"use strict";

const HELPER = require('../../../helper/helper');

class DBInterface{

    constructor(){
    }

    init(){
        console.log('DBInterface init method should add a component that standarices how database CRUD operations are done')
    }
}

module.exports = new DBInterface();
