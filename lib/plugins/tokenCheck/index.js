"use strict";

const HELPER = require('../../../helper/helper');

class tokenCheck{

    constructor(){
    }

    init(){
        console.log('tokenCheck init method should add a component to secure the server using JWT')
    }
}

module.exports = new tokenCheck();
